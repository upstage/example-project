#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-shell/bin/grunt-shell" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-shell/bin/grunt-shell" "$@"
  ret=$?
fi
exit $ret
